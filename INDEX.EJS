<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>ZCL Bot Dashboard</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="bg-dark text-light">
  <div class="container text-center mt-5">
    <% if (user) { %>
      <h1 class="display-4 mb-3">Welcome, <%= user.username %></h1>
      <img src="<%= user.avatar ? `https://cdn.discordapp.com/avatars/${user.id}/${user.avatar}.png` : 'https://cdn-icons-png.flaticon.com/512/147/147144.png' %>" class="rounded-circle mb-3" width="100" height="100" alt="Avatar">
      <div class="d-grid gap-2 col-6 mx-auto mt-4">
        <a class="btn btn-primary" href="/dashboard">Go to Dashboard</a>
        <a class="btn btn-outline-light" href="/logout">Logout</a>
      </div>
    <% } else { %>
      <h1 class="display-5">ZCL Bot Admin Panel</h1>
      <p class="lead">Please log in with your Discord account to continue</p>
      <a class="btn btn-success btn-lg mt-3" href="/login">Login with Discord</a>
    <% } %>
  </div>
</body>
</html>