<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Your Servers - ZCL Bot</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h1>Choose a Server</h1>
    <p>Welcome, <%= user.username %>!</p>
    <div class="server-grid">
      <% guilds.forEach(guild => { %>
        <div class="server-card">
          <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" alt="<%= guild.name %> Icon" onerror="this.src='/default-icon.png'">
          <h2><%= guild.name %></h2>
          <a href="/dashboard/<%= guild.id %>" class="btn">Manage</a>
        </div>
      <% }) %>
    </div>
    <a href="/logout" class="btn logout">Logout</a>
  </div>
</body>
</html>