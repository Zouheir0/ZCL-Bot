<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Select a Server | ZCL Dashboard</title>
  <link rel="stylesheet" href="/styles/style.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #0e0e0e;
      color: white;
      padding: 30px;
    }
    h1 {
      font-size: 2em;
      margin-bottom: 20px;
    }
    .guild {
      background: #1f1f1f;
      padding: 20px;
      margin-bottom: 15px;
      border-radius: 12px;
      display: flex;
      align-items: center;
      transition: all 0.3s ease;
    }
    .guild:hover {
      background: #333;
    }
    .guild img {
      border-radius: 50%;
      margin-right: 15px;
    }
    .guild a {
      color: white;
      text-decoration: none;
      font-size: 1.2em;
    }
  </style>
</head>
<body>
  <h1>Choose a Server</h1>

  <% if (guilds.length === 0) { %>
    <p>You don’t have any servers where you’re an admin and the bot is present.</p>
  <% } else { %>
    <% guilds.forEach(guild => { %>
      <div class="guild">
        <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png?size=64" width="48" height="48" onerror="this.src='https://cdn.discordapp.com/embed/avatars/0.png'">
        <a href="/dashboard/<%= guild.id %>"><%= guild.name %></a>
      </div>
    <% }); %>
  <% } %>
</body>
</html>